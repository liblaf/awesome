---
title: Libraries
---

import { Callout } from "nextra/components";
import { useData } from "nextra/data";
import { github } from "@/lib/github";
import { formatNumber } from "@/lib/format";

export const getStaticProps = async ({ params }) => {
  const GROUPS = {
    "C/C++": ["doxygen/doxygen", "microsoft/vcpkg", "rizsotto/Bear"],
    Go: [
      "dsnet/try",
      "emirpasic/gods",
      "go-survey/survey",
      "gosimple/slug",
      "mholt/archiver",
      "mvdan/sh",
      "otiai10/copy",
      "pterm/pterm",
      "sirupsen/logrus",
      "spf13/cobra",
      "stretchr/testify",
    ],
    Javascript: [
      "ant-design/ant-design",
      "facebook/react",
      "pnpm/pnpm",
      "vercel/next.js",
      "vercel/vercel",
      "vuejs/vue",
      "vuepress-theme-hope/vuepress-theme-hope",
      "vuepress/vuepress-next",
    ],
    LaTeX: ["plk/biber"],
    Python: [
      "benfred/py-spy",
      "conda/conda-lock",
      "conda/conda",
      "jd/tenacity",
      "mamba-org/mamba",
      "matplotlib/matplotlib",
      "mikedh/trimesh",
      "mwaskom/seaborn",
      "Nuitka/Nuitka",
      "numpy/numpy",
      "openai/openai-python",
      "openai/tiktoken",
      "pandas-dev/pandas",
      "Patarimi/typer-cli",
      "psf/requests",
      "pydantic/pydantic",
      "pyinstaller/pyinstaller",
      "pypa/pipx",
      "pytest-dev/pytest",
      "python-poetry/poetry",
      "pyvista/pyvista",
      "pyvista/tetgen",
      "scipy/scipy",
      "taichi-dev/taichi",
      "Textualize/rich",
      "tiangolo/typer",
      "tmbo/questionary",
    ],
    Rust: [
      "ardaku/whoami",
      "chronotope/chrono",
      "clap-rs/clap-verbosity-flag",
      "clap-rs/clap",
      "ConnorGray/clap-markdown",
      "console-rs/indicatif",
      "dtolnay/anyhow",
      "dtolnay/async-trait",
      "EstebanBorai/local-ip-address",
      "mackwic/colored",
      "mikaelmello/inquire",
      "rust-lang/futures-rs",
      "seanmonstar/reqwest",
      "serde-rs/json",
      "serde-rs/serde",
      "soc/directories-rs",
      "tokio-rs/tokio",
      "tokio-rs/tracing",
      "zhiburt/tabled",
    ],
    Shell: [
      "charmbracelet/gum",
      "charmbracelet/vhs",
      "fish-shell/fish-shell",
      "jorgebucaran/fisher",
      "mvdan/sh",
    ],
  };
  const repos = await github(GROUPS);
  return { props: { ssg: { repos: repos } }, revalidate: 86400 };
};

export const Repos = ({ group }) => {
  const { repos } = useData();
  return (
    <table>
      <thead>
        <tr>
          <th> Name </th>
          <th> Stars </th>
          <th> Commits </th>
          <th> Description </th>
        </tr>
      </thead>
      <tbody>
        {repos[group].map((repo) => (
          <tr>
            <td>
              <a href={repo.url}> {repo.name} </a>
            </td>
            <td align="right"> {formatNumber(repo.stars)} </td>
            <td align="right"> {formatNumber(repo.commits)} </td>
            <td> {repo.description} </td>
          </tr>
        ))}
      </tbody>
    </table>
  );
};

<Callout type="info" emoji="ℹ️">
  Commits are counted from the last year only.
</Callout>

## C/C++

<Repos group="C/C++" />

## Go

<Repos group="Go" />

## Javascript

<Repos group="Javascript" />

## LaTeX

<Repos group="LaTeX" />

## Python

<Repos group="Python" />

## Rust

<Repos group="Rust" />

## Shell

<Repos group="Shell" />
